bs:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: user
    topic:
      notification: notificationTopic
      user-sign-up: userSignUpTopic
      user-activity-log: userActivityLogTopic
      template-value: templateValueTopic

